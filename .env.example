# Momentum Web — environment
# Point at the NestJS backend to enable server sync + real AI Coach.
# Leave unset to run fully offline (LocalAdapter + local coach).
#
# Development: NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1
# Production:  NEXT_PUBLIC_API_URL=https://api.momentum.app/v1
NEXT_PUBLIC_API_URL=
